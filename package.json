{
  "name": "dsh-desktop",
  "version": "0.1.0",
  "description": "DSH Desktop - cross-platform Tauri 2 launcher for the DeepSeek Harness web UI",
  "private": true,
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
